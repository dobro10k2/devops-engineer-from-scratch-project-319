#cloud-config

package_update: true
package_upgrade: true

packages:
  - curl

runcmd:
  # Установка k3s агента и подключение к мастеру по его внутреннему IP
  - curl -sfL https://get.k3s.io | K3S_URL="https://${master_priv_ip}:6443" K3S_TOKEN="${k3s_token}" sh -
