apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: yandex-lockbox
spec:
  provider:
    yandexlockbox:
      auth:
        authorizedKeySecretRef:
          name: yc-auth
          key: authorized-key
