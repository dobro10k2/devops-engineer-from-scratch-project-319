apiVersion: v1
kind: Service
metadata:
  name: bulletin-app
spec:
  type: ClusterIP
  selector:
    app: bulletin-app
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  - port: 9090
    targetPort: 9090
    name: metrics
